import notesIcon from '@/assets/notes.png'

const appList: [{ name: string, displayName: string, icon: string, path: string }] = [
  {
    name: 'notes',
    displayName: 'Notes',
    icon: notesIcon,
    path: '/notes'
  },
]

export default appList